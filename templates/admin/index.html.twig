{% extends 'base.html.twig' %}

{% block title %}{{ site_name }}: Admin{% endblock %}

{% block body %}

<main id="main">

    <!-- ======= Blog Section ======= -->
    <section class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>Admin</h2>

                <ol>
                    <li><a href="{{ path('home') }}">Accueil</a></li>
                    <li>Admin Index</li>
                </ol>
            </div>

        </div>
    </section><!-- End Blog Section -->

    <!-- ======= Blog Section ======= -->
    <section id="blog" class="blog">
        <div class="container" data-aos="fade-up">

            <h2 class="text-center fw-bold card py-2 mb-4">DASHBOARD</h2>

            <div class="row d-flex justify-content-center mb-5">

                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">ARTICLES</h5>
                            <a href="{{ path('article_index') }}" class="btn btn-primary fw-bold p-3 fs-6">Liste des Articles</a>
                            <br>
                            <a href="{{ path('article_new') }}" class="btn btn-success fw-bold p-3 fs-6">Créer un  article</a>
                        </div>
                    </div>
                </div>
                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">CATEGORIES</h5>
                            <a href="{{ path('category_admin_index') }}" class="btn btn-info fw-bold p-3 fs-6">Liste des Catégories</a>
                            <br>
                            <a href="{{ path('category_new') }}" class="btn btn-success fw-bold p-3 fs-6">Créer une categorie</a>
                        </div>
                    </div>
                </div>
                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">TAGS</h5>
                            <a href="{{ path('tag_admin_index') }}" class="btn btn-warning fw-bold p-3 fs-6">Liste des Tags</a>
                            <br>
                            <a href="{{ path('tag_new') }}" class="btn btn-success fw-bold p-3 fs-6">Créer un tagtag</a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row d-flex justify-content-center mb-5">
                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">COMMENTAIRES</h5>
                            <a href="{{ path('comment_admin_index') }}" class="btn btn-warning fw-bold p-3 fs-6">Liste des Commentaires</a>
                            <br>
                            <a href="" class="btn btn-outline-success fw-bold p-3 fs-6">Autres</a>
                        </div>
                    </div>
                </div>
                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">UTILISATEURS</h5>
                            <a href="{{ path('user_index') }}" class="btn btn-warning fw-bold p-3 fs-6">Liste des utilisateurs</a>
                            <br>
                            <a href="{{ path('user_new') }}" class="btn btn-success fw-bold p-3 fs-6">Créer un utilisateur</a>
                        </div>
                    </div>
                </div>
                <div class="col">
                    <div class="card h-100">
                        <div class="d-grid gap-2 card-body">
                            <h5 class="card-title fw-bold">LIENS</h5>
                            <a href="{{ path('link_admin_index') }}" class="btn btn-warning fw-bold p-3 fs-6">Liste des liens</a>
                            <br>
                            <a href="{{ path('link_new') }}" class="btn btn-success fw-bold p-3 fs-6">Créer un lien</a>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </section>
</main>

{% endblock %}