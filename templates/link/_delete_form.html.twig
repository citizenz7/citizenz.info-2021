<form method="post" action="{{ path('link_delete', {'id': link.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer ce lien?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ link.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
