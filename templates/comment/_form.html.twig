{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="my-3">
        <button class="btn btn-sm btn-success">{{ button_label|default('Enregistrer') }}</button> 
        <button class="btn btn-sm btn-secondary" type="reset">Annuler</button> 
        <a class="btn btn-sm btn-primary" href="{{ path('comment_admin_index') }}">Retour à la liste des commentaires</a>
    </div>
{{ form_end(form) }}
